<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ReportMapper">
	<insert id="insertReport" parameterType="com.groo.model.ReportDTO">
		INSERT INTO REPORT 
		(
			REPORT_ID,
			MEM_ID,
			BOARD_ID,
			COMENT_ID,
			REPORT_CONTENT,
			REPORT_DATE
		)VALUES(
			'R'||REPORT_SEQ.NEXTVAL,
			(SELECT MEM_ID FROM MEMBER WHERE MEM_LOGIN_ID = #{memReporter}),
			#{boardId},
			#{comentId},
			#{reportContent},
			SYSDATE
		)
	</insert>
	<select id="memberSelectBeenCheck" parameterType="com.groo.model.ReportDTO" resultType="com.groo.model.ReportDTO">
		SELECT
		    REPORT_CONTENT,
		    ACTION_DATE,
		    ENDACTION_DATE,
		    CASE
		        WHEN ENDACTION_DATE <![CDATA[ >= ]]> TRUNC(SYSDATE) THEN 'Y'
		        ELSE 'N'
		    END AS BEEN_COUNT
		FROM
		    REPORT
		WHERE
		    MEM_ID = (SELECT MEM_ID FROM MEMBER WHERE MEM_LOGIN_ID = #{memLoginId})
	</select>
</mapper>