<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserMapper">

	<insert id="insertMember" parameterType="com.groo.model.MemberDTO">
		<selectKey keyProperty="memId" resultType="String" order="BEFORE">
           SELECT 'mem_no_' || MEMBER_SEQ.NEXTVAL FROM DUAL
        </selectKey>
	  INSERT INTO MEMBER (
	  						MEM_ID,
	  						MEM_NAME,
	  						MEM_LOGIN_ID, 
	  						MEM_PASS, 
 	  						MEM_BIRTH,
	  						MEM_GENDER, 
	  						MEM_EMAIL,
	  						MEM_NICK
	  						)
	  			  values (
	  			 			'M' || MEMBER_SEQ.NEXTVAL
	  			 			#{memName}, 
	  			 			#{memLoginId}, 
	  			 			#{memPass}, 
 	  			 			#{memBirth},
	  			 			#{memGender}, 
	  			 			#{memEmail},
	  			 			'USER_' || MEMBER_SEQ.NEXTVAL
	  					    )
	</insert>
	
	<select id="selectEmail" parameterType="String" resultType="String">
	  select MEM_EMAIL from member where MEM_EMAIL=#{memEmail}
	</select>
	
	<update id="userDelete" parameterType="com.groo.model.MemberDTO">
		UPDATE member
        SET MEM_STATUS = 'Y',
        	MEM_OUT_DATE = SYSDATE
        WHERE MEM_LOGIN_ID = #{memLoginId}
	</update>
	
	<update id="userUpdate" parameterType="com.groo.model.MemberDTO">
		UPDATE member
        SET MEM_NICK = #{memNick},
            MEM_PASS = #{memPass},
            MEM_EMAIL = #{memEmail},
            MEM_ADDR = #{memAddr},
            MEM_INTEREST = #{memInterest}
        where MEM_LOGIN_ID = #{memLoginId}
	</update>
	
<!-- 닉네임 중복 확인 -->
	<select id="selectUserNicknameCheck" parameterType="com.groo.model.MemberDTO" resultType="com.groo.model.MemberDTO">
	  	SELECT MEM_NICK 
        FROM MEMBER 
        WHERE MEM_NICK= #{memNick};
	</select>
	
	<select id="selectLoginUser" parameterType="com.groo.model.MemberDTO" resultType="com.groo.model.MemberDTO">
		SELECT *
		FROM MEMBER
		WHERE MEM_LOGIN_ID = #{memLoginId} and MEM_PASS = #{memPass}
	</select>
	
	<select id="selectLoginId" parameterType="com.groo.model.MemberDTO" resultType="com.groo.model.MemberDTO">
	  select MEM_LOGIN_ID from member where MEM_LOGIN_ID=#{memLoginId}
	</select>
	
	<select id="selectSocialLoginCheck" parameterType="com.groo.model.MemberDTO" resultType="com.groo.model.MemberDTO">
		SELECT MEM_LOGIN_ID 
		FROM MEMBER 
		WHERE MEM_LOGIN_ID = #{memLoginId}
	</select>
	
	<insert id="insertSociallMember" parameterType="com.groo.model.MemberDTO">
		INSERT INTO MEMBER	
		(
			MEM_ID,
			MEM_LOGIN_ID,
			MEM_NAME,
			MEM_LOGIN_TOKEN,
			MEM_NICK
		)VALUES(
			'M' || MEMBER_SEQ.NEXTVAL,
			#{memLoginId},
			#{memName},
			#{memLoginId},
			'SociallUser_' || MEMBER_SEQ.NEXTVAL
		)
	</insert>
	<select id="selectLoginMember" parameterType="com.groo.model.MemberDTO" resultType="com.groo.model.MemberDTO">
		SELECT 
			MEM_LOGIN_ID,
			MEM_PASS
		FROM MEMBER
		WHERE
			MEM_LOGIN_ID = #{memLoginId} AND
			MEM_PASS = #{memPass}
	</select>
	
	
</mapper>